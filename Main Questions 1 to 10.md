# Beginner Task — Theory Answers

## 1- What is the difference between mean, median and mode?
-  Mean is the avrage of all values (sum of number / number count)
- Median The middel value after sorting all value
- Mode is the value is most freq in the values

## 2-What is the definition of P value in statistics
- P value is the probablity result of obtaining the observed result

## 3 -What is the difference between left and right skewness of the data

-left skew is  tail on left data mostly right
-right skew is tail on right  data mostly left

## 4- What is the difference between Skewness and Kurtosis?
-skewness measures the asymmetry of a distribution
-kurtosis measures the tailedness or peak sharpness of a distribution



 ## 5-What is the difference between list and tuples in Python

 - list is mutable can be change  and flexible uses []
 - tuple is immutable cant not be change and its fixed uses()

 ## 6-How can you generate random numbers in Python

 - import random 
   x=random.randint(1,200)
   print(x)

## 7-What is break, continue and pass in Python
- break exits the current loop 
- continue skips the current iteration and moves to the next iteration
- pass does nothing used as palceholder

## 8-How can you reverse all the items in a list in Python using just one line of code?
- _list=[1,2,3,4,5,6,7,8]
  _list=_list[::-1]
  print(_list)

## 9-Given a list of integers numbers = [2, 7, 3, 9, 5], write a Python code snippet to find and print the maximum value in the lis
- numbers=[2, 7, 3, 9, 5]
  print(max(numbers))

## 10 What is an outlier in a dataset, and how would you explain it to someone who is new to statistics
- outlier is a data point that is significantly higher or lower than most of the other values
sush as in [18,19,20,18,19,50] the outlier is 50 because is not follow the pattern of this numbers 



 
